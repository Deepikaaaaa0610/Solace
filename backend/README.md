# Backend Scaffold

This repo is still frontend-first. The `backend` folder contains the production design artifacts needed to add the WhatsApp poetry ingestion service without inventing the architecture later.

- `contracts/whatsapp-poetry-api.md`: endpoint contract
- `schema/001_whatsapp_poetry.sql`: PostgreSQL schema draft
